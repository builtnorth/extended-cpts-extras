<?php
/**
 * Single source of truth for this package's version.
 *
 * Read via a bare require before any autoloader is involved — package-loader
 * compares bundled copies before deciding whose classes to resolve, so this
 * cannot live behind a class constant. Written by the release workflow's
 * version-stamping step.
 *
 * @package ExtendedCPTsExtras
 */

return '1.2.4';
